# 📦 Anajak Superapp - Database Setup

## วิธีติดตั้ง Database Schema

### 1. เปิด Supabase SQL Editor
1. ไปที่ https://supabase.com/dashboard
2. เลือก Project ของคุณ
3. ไปที่ **SQL Editor** (เมนูซ้าย)

### 2. รัน SQL Schema
1. คัดลอกเนื้อหาทั้งหมดจากไฟล์ `schema.sql`
2. วางใน SQL Editor
3. คลิก **Run** หรือกด `Ctrl + Enter`

### 3. ตรวจสอบว่าสร้างสำเร็จ
ควรจะมี Tables เหล่านี้:

#### Core System (ระบบกลาง)
- `roles` - บทบาทผู้ใช้
- `user_profiles` - ข้อมูลผู้ใช้
- `audit_logs` - บันทึกการกระทำ
- `notifications` - การแจ้งเตือน
- `notification_settings` - ตั้งค่าการแจ้งเตือน

#### Warehouse Module (คลังสินค้า)
- `products` - สินค้า (มีอยู่แล้ว)
- `transactions` - ธุรกรรมสต๊อก (มีอยู่แล้ว)
- `suppliers` - ซัพพลายเออร์

#### Production Module (การผลิต)
- `production_jobs` - งานผลิต
- `production_job_items` - รายการสินค้าในงาน
- `stock_reservations` - การจองสต๊อก
- `production_updates` - อัปเดตสถานะ
- `qc_logs` - บันทึก QC
- `defect_types` - ประเภทของเสีย

#### CRM Module (ลูกค้าสัมพันธ์)
- `customers` - ลูกค้า
- `customer_contacts` - ผู้ติดต่อ
- `customer_interactions` - ประวัติการติดต่อ

---

## 🔐 Default Roles (สิทธิ์เริ่มต้น)

| Role | คำอธิบาย | สิทธิ์ |
|------|---------|--------|
| `super_admin` | ผู้ดูแลระบบ | เข้าถึงทุกส่วน |
| `stock_admin` | ผู้จัดการสต๊อก | จัดการสต๊อกและสินค้า |
| `production_admin` | ผู้จัดการผลิต | จัดการงานผลิตและ QC |
| `sales_admin` | ผู้จัดการขาย | จัดการลูกค้าและ CRM |
| `operator` | พนักงานปฏิบัติการ | ดูข้อมูลและทำรายการ |
| `viewer` | ผู้ดูเท่านั้น | ดูข้อมูลอย่างเดียว |

---

## 📱 การแจ้งเตือนผ่าน LINE Messaging API

### ขั้นตอนที่ 1: สร้าง LINE Official Account
1. ไปที่ https://developers.line.biz/console/
2. สร้าง Provider ใหม่ (หรือใช้ที่มีอยู่)
3. สร้าง **Messaging API Channel**
4. ตั้งค่า Channel ให้เรียบร้อย

### ขั้นตอนที่ 2: ดึง Channel Access Token
1. ไปที่ Channel ที่สร้าง → **Messaging API** tab
2. กด **Issue** ที่ส่วน "Channel access token (long-lived)"
3. คัดลอก Token ไว้

### ขั้นตอนที่ 3: เพิ่ม Config ใน Database
รันคำสั่งนี้ใน Supabase SQL Editor:

```sql
INSERT INTO line_config (channel_access_token, channel_secret) 
VALUES (
  'YOUR_CHANNEL_ACCESS_TOKEN',
  'YOUR_CHANNEL_SECRET'
);
```

### ขั้นตอนที่ 4: รับ LINE User ID ของผู้ใช้
มี 2 วิธี:
1. **LINE Login** - ผู้ใช้ Login ด้วย LINE แล้วระบบจะได้ User ID อัตโนมัติ
2. **Webhook** - ผู้ใช้ Add Bot เป็นเพื่อน แล้วส่งข้อความมา ระบบจะได้ User ID จาก Webhook

### ⚠️ หมายเหตุ
- LINE Messaging API มีโควต้าฟรี 500 ข้อความ/เดือน
- ถ้าต้องการส่งมากกว่านั้น ต้องอัพเกรดเป็น Premium Plan
- LINE User ID จะขึ้นต้นด้วย "U" เช่น `U1234567890abcdef...`

---

## 🚀 หลังจากติดตั้งแล้ว

1. สร้างผู้ใช้คนแรก (Super Admin) ผ่านหน้า `/users`
2. ตั้งค่า Notification ในหน้า Settings
3. เริ่มใช้งานได้เลย!

---

## ⚠️ หมายเหตุ

- Schema นี้ใช้ **Row Level Security (RLS)** สำหรับบาง Tables
- **Realtime** เปิดใช้งานสำหรับ `notifications`, `production_jobs`, `production_updates`
- ฟังก์ชัน `generate_job_number()` และ `generate_customer_code()` ใช้สร้างเลขอัตโนมัติ

