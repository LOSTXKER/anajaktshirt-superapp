# Project Plan: Anajak Stock Manager (Next.js + Supabase + AI)

**เจ้าของโครงการ:** Anajak T-Shirt Factory (DTF & DTG Screening)
**เป้าหมาย:** พัฒนาระบบจัดการสต๊อกสินค้าออนไลน์ เพื่อจัดการสต๊อกเสื้อเปล่า (Hiptrack, Gildan ฯลฯ) รองรับการทำงานหลายคน ข้อมูลเรียลไทม์ และใช้ AI ช่วยวิเคราะห์เพื่อลดต้นทุน

## 🛠️ 1. Tech Stack (เครื่องมือที่จะใช้)

- **Frontend Framework:** [Next.js 14+](https://nextjs.org/) (ใช้ App Router)
- **Styling:** [Tailwind CSS](https://tailwindcss.com/) (จัดหน้าตาสวยงาม)
- **UI Components:** [shadcn/ui](https://ui.shadcn.com/) (Component สำเร็จรูป)
- **Icons:** [Lucide React](https://lucide.dev/)
- **Backend & Database:** [Supabase](https://supabase.com/) (Open Source Firebase Alternative)
    - **Database:** PostgreSQL (ฐานข้อมูลแบบตาราง ที่มีความสัมพันธ์ชัดเจน)
    - **Auth:** ระบบล็อคอิน (Email/Password)
    - **Realtime:** อัปเดตข้อมูลทันทีเมื่อมีการเปลี่ยนแปลง
- **AI & Integration:**
    - **OpenAI API (GPT-4o) / Gemini:** สำหรับวิเคราะห์ข้อมูลและ Chatbot
    - **Python (Optional):** สำหรับรัน Model ทำนายยอดขาย (Forecasting)

## 🗄️ 2. Database Design (โครงสร้างฐานข้อมูล SQL)

เปลี่ยนมาใช้ PostgreSQL ซึ่งเหมาะกับการคำนวณสต๊อกและรายงานที่แม่นยำ

### 📌 Table: `products` (สินค้า)

เก็บข้อมูล Master Data ของเสื้อ

| Column | Type | Description |
| --- | --- | --- |
| `id` | `uuid` | Primary Key |
| `code` | `text` | SKU (Unique) เช่น S003-S1 |
| `name` | `text` | ชื่อรุ่น (เสื้อ Hiptrack) |
| `color` | `text` | สี |
| `size` | `text` | ไซส์ |
| `cost` | `numeric` | ต้นทุน |
| `price` | `numeric` | ราคาขาย |
| `stock` | `integer` | จำนวนคงเหลือ (Default: 0) |
| `supplier_id` | `uuid` | **Foreign Key** (Optional) แหล่งที่มาสินค้า |
| `low_stock_threshold` | `integer` | จุดสั่งซื้อ (Reorder Point) |
| `created_at` | `timestamptz` | วันที่สร้าง |

### 📌 Table: `transactions` (ประวัติการทำรายการ)

เก็บ Transaction ทุกอย่าง โดยเชื่อมโยงกับตาราง products

| Column | Type | Description |
| --- | --- | --- |
| `id` | `uuid` | Primary Key |
| `created_at` | `timestamptz` | วันที่ทำรายการ (Default: now()) |
| `type` | `text` | 'in' (รับเข้า), 'out' (เบิกออก) |
| `category` | `text` | 'Stock In', 'Sale', 'Defect', 'Adjust' |
| `product_id` | `uuid` | **Foreign Key** เชื่อมกับ products.id |
| `product_name` | `text` | (Snapshot) ชื่อสินค้า ณ ตอนนั้น |
| `qty` | `integer` | จำนวน |
| `cost_per_unit` | `numeric` | ต้นทุนต่อหน่วย ณ ตอนนั้น |
| `price_per_unit` | `numeric` | ราคาขายต่อหน่วย ณ ตอนนั้น |
| `reason` | `text` | สาเหตุ (เช่น สกรีนผิดลาย) |
| `order_ref` | `text` | **(New)** เลข Order อ้างอิง (เช่น ORD-1024) |
| `note` | `text` | หมายเหตุเพิ่มเติม |
| `defect_image_url` | `text` | **(New)** รูปภาพประกอบของเสีย (สำหรับ AI) |
| `user_id` | `uuid` | ใครเป็นคนทำรายการ |

### 📌 Table: `settings` (ตั้งค่าระบบ)

เก็บ Config ต่างๆ

| Column | Type | Description |
| --- | --- | --- |
| `key` | `text` | Primary Key (เช่น 'defect_reasons') |
| `value` | `jsonb` | เก็บข้อมูลแบบ JSON |

## 🚀 3. Phase Plan (ลำดับการพัฒนา)

### **Phase 1: Setup & Database (1 สัปดาห์)**

- [ ]  สร้างโปรเจค Next.js + Tailwind
- [ ]  สร้าง Project ใน Supabase Dashboard
- [ ]  **Run SQL Scripts:** สร้างตาราง products, transactions ตาม Design
- [ ]  สร้าง Typescript Database Definition (เพื่อให้ Code รู้จักโครงสร้าง DB)

### **Phase 2: Core Functions (1-2 สัปดาห์)**

- [ ]  **ระบบ Login:** เชื่อมต่อ Supabase Auth
- [ ]  **หน้า Inventory:**
    - CRUD (Create, Read, Update, Delete) ข้อมูลสินค้า
    - เชื่อมต่อตาราง `products`
- [ ]  **ระบบตัดสต๊อก (Transaction Logic):**
    - เขียน API หรือ Server Action ที่ทำ 2 อย่างใน Transaction เดียว (Database Transaction):
        1. `INSERT` ลงตาราง `transactions`
        2. `UPDATE` ค่า `stock` ในตาราง `products`

### **Phase 3: User Interface & Wizard (1 สัปดาห์)**

- [ ]  **หน้า In/Out (Wizard Step):**
    - Step 1: เลือกรุ่น (Query distinct names)
    - Step 2: เลือกสี (Query distinct colors where name = X)
    - Step 3: เลือกไซส์ (Query products where name = X AND color = Y)
- [ ]  **หน้า History:**
    - Query ข้อมูลจาก `transactions` join กับ `products`
    - เพิ่ม Filter กรองตาม "เลข Order" หรือ "สาเหตุการเสีย"

### **Phase 4: Advanced & Deploy (1 สัปดาห์)**

- [ ]  **Import/Export:** ระบบ CSV
- [ ]  **Dashboard:** เขียน SQL Query เพื่อนับยอดรวม และกำไร
- [ ]  **Realtime:** เปิดใช้ Supabase Realtime Subscription ให้หน้าจอ Inventory อัปเดตเองเวลามีคนตัดสต๊อก
- [ ]  **Deploy:** Vercel

### **Phase 5: Smart Notification (1 สัปดาห์)**

- [ ]  **LINE Notify Alert:** แจ้งเตือนเข้าไลน์กลุ่มเมื่อ:
    - สต๊อกรุ่นไหนต่ำกว่าจุดสั่งซื้อ (Low Stock)
    - มีการเบิกสินค้าเสียเกิน 5 ตัว/วัน (แจ้งเตือนความผิดปกติ)
- [ ]  **Supplier Linking:** เก็บข้อมูลร้านขายเสื้อเปล่า (เบอร์โทร, ไลน์) เวลากดดูสินค้าที่ใกล้หมด จะได้กดติดต่อสั่งของได้ทันที

### **Phase 6: AI & Automation (Future Integration) 🤖**

- [ ]  **🔮 AI Demand Prediction:**
    - ใช้ประวัติการใช้งานย้อนหลัง 3-6 เดือน มาวิเคราะห์ Trend
    - ทำนายว่า "เดือนหน้าต้องสั่ง Hiptrack ดำ L กี่ตัว" เพื่อไม่ให้ของขาด/จมทุน
- [ ]  **📸 AI Visual Inspection (สแกนของเสีย):**
    - เพิ่มปุ่มอัปโหลดรูปตอนแจ้งของเสีย
    - ใช้ AI (Vision Model) วิเคราะห์รูปเพื่อระบุประเภทความเสียหายอัตโนมัติ (เช่น คราบกาว DTF, รูผ้า, สีเพี้ยน)
    - ช่วยเก็บหลักฐานเพื่อเคลม Supplier หรือประเมินพนักงาน
- [ ]  **💬 Smart Assistant (Natural Language Query):**
    - พิมพ์ถามระบบเป็นภาษาไทย: *"ขอสรุปยอดเสียเดือนนี้ แยกตามสาเหตุ"*
    - AI แปลงคำถามเป็น SQL Query ดึงข้อมูลจาก Supabase มาแสดงเป็นกราฟหรือข้อความสรุป